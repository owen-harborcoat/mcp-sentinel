# Demo values only. Uvicorn --env-file .env loads these explicitly.
HELIX_MODE=clean
HELIX_HOST=127.0.0.1
HELIX_PORT=8765
SENTINEL_HOST=127.0.0.1
SENTINEL_PORT=8787
SENTINEL_ENFORCE=1
HELIX_UPSTREAM=http://127.0.0.1:8765/mcp
FAKE_SSH_KEY=not-a-real-key
SQLITE_PATH=sentinel/sentinel.db
SEED_PATH=sentinel/seed.json
